
## Environment addendum — this machine (Nix flake + Nushell)

This `/improve` install runs inside a Nix-flake-managed dotfiles repo, and the
user's interactive shell is **Nushell**. These notes override the generic
defaults above wherever they conflict.
